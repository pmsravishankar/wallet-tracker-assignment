import React from 'react';

const NavBar = () => (
    <nav>
        <div className="nav-wrapper grey darken-4">
        <a href="/" className="brand-logo center">Wallet Tracker App</a>
        </div>
    </nav>
);

export default NavBar;